server:
  port: 8080

# Configurações do Banco de dados
spring:
  datasource:
    url: jdbc:postgresql://dpg-ckvf3dmb0mos7397h3o0-a.oregon-postgres.render.com/credenciais
    username: renderpostgres
    password: dXQdih2FZgFhNCBqvHBETBjS2Dau11b8
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

# Configurações do Token JWT
api:
  security:
    token:
      user:
        secret: ${JWT_USER_SECRET:SENHA_SECRETA_PARA_VALIDAR_AUTENTICACAO_DO_USUARIO}
      apigateway:
        secret: ${JWT_GATEWAY_SECRET:SENHA_SECRETA_PARA_VALIDAR_API_GATEWAY}
      mscadastro:
        secret: ${JWT_MS_CADASTRO_SECRET:SENHA_SECRETA_PARA_VALIDAR_MS_CADASTRO}
  gateway:
    issuer: API GATEWAY SPORTEASE UFPR
  user:
    issuer: USER ISSUER SPORTEASE UFPR
  mscadastro:
    issuer: API MS CADASTRO SPORTEASE UFPR

# URLs
url:
  ms:
    cadastros:
      adm: http://localhost:8081/adm
      clientes: http://localhost:8081/clientes
      espacos_esportivos: http://localhost:8081/espacos-esportivos
      esportes: http://localhost:8081/esportes
      usuarios: http://localhost:8081/usuarios
    locacoes: http://localhost:8082/locacoes
